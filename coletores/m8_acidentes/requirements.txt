# Módulo 8 — coletor de Acidentes e Emergências Ambientais da SEMAD
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pdfplumber==0.11.4      # texto nativo de PDF
pdf2image==1.17.0        # renderiza página como imagem quando não há texto nativo
pytesseract==0.3.13      # OCR (precisa do binário tesseract instalado no runner)
supabase==2.9.1          # client oficial — grava via service_role key (nunca a anon key)
python-dateutil==2.9.0.post0
